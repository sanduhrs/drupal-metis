metis_default_server:
  type: string
  label: Default server
metis_force_ssl:
  type: boolean
  label: Force SSL
field.value.metis:
  type: mapping
  label: Default value
  mapping:
    code_public:
      type: string
      label: Public code
    code_private:
      type: string
      label: Private code
    server:
      type: string
      label: Server
    used:
      type: string
      label: used
